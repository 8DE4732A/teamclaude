{
  "name": "teamclaude",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "pnpm -r test",
    "test:e2e": "pnpm --dir apps/server exec vitest run --root ../.."
  }
}
