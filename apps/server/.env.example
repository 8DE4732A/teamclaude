# Server runtime
NODE_ENV=development
PORT=3000

# API / CORS
API_BASE_URL=http://localhost:3000
WS_PATH=/socket.io

# Auth0 / OIDC
OIDC_ISSUER=https://dev-tp41zg2u1tbyc1yi.us.auth0.com/
OIDC_CLIENT_ID=051fmYAXzyptyi91EiLEfQXoy2ddwhtM
OIDC_CLIENT_SECRET=
OIDC_CALLBACK_URL=http://localhost:3000/auth/callback

# CLI auth callback (must be added to Auth0 Allowed Callback URLs)
OIDC_CLI_CALLBACK_URL=http://localhost:3000/auth/cli/callback

# Session
SESSION_SECRET=change-this-to-a-random-string

# JWT signing secret for CLI/plugin tokens (defaults to SESSION_SECRET if unset)
JWT_SECRET=

# Single-tenant MVP: browser 用户的默认租户
DEFAULT_TENANT_ID=default

# Static file serving (path to web build output)
# Default: ../web/dist relative to server src
# STATIC_ROOT=
