version: '3.9'
services:
  mongo:
    image: mongo:7
    container_name: teamclaude-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7-alpine
    container_name: teamclaude-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

  server:
    image: node:20-alpine
    container_name: teamclaude-server
    working_dir: /workspace
    restart: unless-stopped
    command: >
      sh -c "corepack enable && pnpm install && pnpm --dir apps/server start"
    env_file:
      - ./apps/server/.env
    environment:
      PORT: 3000
    volumes:
      - ./:/workspace
    depends_on:
      - mongo
      - redis
    ports:
      - '3000:3000'

  web:
    image: node:20-alpine
    container_name: teamclaude-web
    working_dir: /workspace
    restart: unless-stopped
    command: >
      sh -c "corepack enable && pnpm install && pnpm --dir apps/web dev"
    env_file:
      - ./apps/web/.env
    volumes:
      - ./:/workspace
    ports:
      - '5173:5173'
    depends_on:
      - server

volumes:
  mongo-data:
  redis-data:
